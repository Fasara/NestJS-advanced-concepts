## IoC container

Inversion of control is a principle where the framework takes over the responsibility of managing the lifecycle and dependencies in the code, rather than doing it manually.

NestJS uses a built a in **IoC container** to manage the application's classes; it's like a manager that mange the object creation and provide their instances according to the module provider or dependencies.